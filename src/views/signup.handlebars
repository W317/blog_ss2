<div class="row">

    <div class="col-md-4 col-md-offset-4">

        <h1>Sign Up</h1>
        {{!--{{#if hasErrors}}
        <div class="alert alert-danger">
            {{# each messages}}
            <p>{{this}}</p>
            {{/each}}
        </div>
        {{/if}}--}}
        <form action="/user/signup" method="post">

            <div class="form-group">
                <label for="text">First name</label>
                <input type="text" id="fname" name="fname" placeholder=" first name" title="enter your firstname" class="form-control">
            </div>

            <div class="form-group">
                <label for="text">Last name</label>
                <input type="text" id="lname" name="lname" placeholder=" last name" title="enter your lastname" class="form-control">
            </div>

            <div class="form-group">
                <label for="email">E-Mail or Phone</label>
                <input type="text" id="email" name="email or phone" placeholder=" email or phone" title="enter your email or phone" class="form-control">
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" placeholder=" password" title="enter your password" class="form-control">
            </div>
            
            <p>Already a member? <a href="/pages/signin">Sign in</a></p>

            <input type="hidden" name="_csrf" value="{{ csrfToken }}">

            <button type="submit" class="btn btn-primary">Sign Up</button>

        </form>

    </div>

</div>