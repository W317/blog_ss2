
	<section id="latest-blog" class="scrollspy-section padding-large">
		<div class="container">
			<!-- tạo 1 array chứa các array nhỏ chứa tối đa 3 blog/array nhỏ -->
      {{#each blogarrays}}
			<div class="row">
				<div class="col-md-12">
					<!-- post grid -->
					<div class="post-grid">
						<div class="row">
							{{#each blogarrays.blog}}
							<div class="col-md-4 post-item">
	            <figure>
	              <img src={{image}} alt="jewellery" class="post-image">
	            </figure>
	            <div class="post-content">
	              <div class="meta-tags">
	                <span class="meta-date">Posted in {{date}}</span>
	                <span class="meta-date">by {{author}}</span>
	              </div>
	              <h3 class="post-title">
	                <a href="#">{{title}}</a>
	              </h3>
	              <p>First we need to check all the luxury colors of the jewels whic</p>
	            </div>
	          </div>
            {{/each}}
						</div>
					</div>
				</div>

			</div>
      {{/each}}
	</div>
</section>